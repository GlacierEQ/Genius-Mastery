env:
  PIP_DISABLE_PIP_VERSION_CHECK: "1"

agents:
  queue: macos-self

steps:
  - label: ":test_tube: Mastery Buildkite bisect 0 — pure runner"
    key: bisect-pure-runner
    timeout_in_minutes: 5
    command: |
      set -eu
      echo "PURE_RUNNER_OK"
      uname -a
