[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "genius-mastery"
version = "1.1.0"
description = "Genius family kernel for synthesis, instruction engineering, durable progress, mission intelligence, evidence, composition, runtime discovery, and verification"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "GlacierEQ" }]
dependencies = [
  "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
genius = "genius.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
genius = ["templates/**/*"]
